[project]
name = "nightwatch-guardroom-backend"
version = "0.1.0"
description = "NightWatch graph aggregation and frontend API"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115,<1",
    "httpx>=0.27,<1",
    "jsonschema>=4.23,<5",
    "uvicorn>=0.30,<1",
]

[dependency-groups]
dev = ["httpx>=0.27,<1"]
