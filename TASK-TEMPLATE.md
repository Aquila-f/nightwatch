# 任務檔的寫法:只定目標,不寫做法

任務檔是當天實作的人拿去餵 codex 的「一段目標」。它**只說要達成什麼、怎麼算達成**;細節(欄位、名字、公式、字串)一律指到 `contracts/` 的章節,不抄進來。理由:任務檔要像一個人當天寫得出來的目標,不像從既有程式碼抄出來的規格;細節放契約,契約是團隊事前設計好的介面,本來就該詳細。

一份任務檔 20 到 35 行,固定七節:

```
# <部份> <NN>:<一句話的目標>

## 為什麼要有這段
一到兩句:這段在整個 demo 裡扮演什麼,沒有它會怎樣。

## 目標
三到六條。每條寫「做完之後會怎樣」,不寫「怎麼做」。
不列端點清單、不列欄位、不抄 log 字串、不寫套件名、不寫演算法步驟。
可以寫數字門檻(「每 5 秒一張」「三次連續」),因為那是目標的一部份。

## 契約在哪
指到 `../contracts/<檔>` 的章節與 schema 檔名。一行一個。只指,不抄。

## 怎麼算做完
三到五條,結果導向:「起來之後 <某個檢查腳本> 全綠」「按下去 X 秒內看到 Y」。
可以給一條起服務的指令,不要逐行寫預期輸出。console 的段一定要有「瀏覽器裡開、截圖存 .shots/」。

## 測試
一句:只寫契約邊界與 demo 主線。最多列三個「一定要有的」測試,用一句話描述各自守什麼。

## 不做什麼
兩三條,擋住越界。

## 接線點
一句:這段做完可以跟誰接、用什麼接、要看到什麼(對照 README §2 的階梯)。
```

判斷「太細」的方法:讀完任務檔,如果不看契約就能寫出程式,就是太細了;任務檔應該讓人知道**要去契約的哪裡找**,而不是替他找好。反過來,如果契約裡沒有那個細節,補進契約,不要補進任務檔。

回報格式照各部份 `BRIEF.md` 最後一節:做了什麼、依據什麼驗證、沒驗證什麼、契約疑問。
