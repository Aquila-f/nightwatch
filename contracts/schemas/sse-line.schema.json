{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "NightWatch events.jsonl SSE line",
  "type": "object",
  "required": ["event", "data"],
  "properties": {
    "event": { "enum": ["state", "incident", "graph", "faults", "readiness", "run"] },
    "data": { "type": "object", "additionalProperties": true }
  },
  "additionalProperties": false
}
