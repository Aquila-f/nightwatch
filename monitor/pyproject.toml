[project]
name = "nightwatch-monitor"
version = "0.1.0"
description = "Python instrumentation SDK for NightWatch"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/monitor"]
